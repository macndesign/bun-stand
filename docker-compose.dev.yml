services:
  app:
    build: !reset null
    image: oven/bun:1
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
      - data:/data
    command: sh -c "bun install --frozen-lockfile --ignore-scripts && bun run dev"