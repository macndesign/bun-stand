# Copy this file to .env and fill in a real secret before running the app.
# Generate one with: bunx @better-auth/cli@latest secret

BETTER_AUTH_SECRET=replace-with-a-long-random-string
BETTER_AUTH_URL=http://localhost:3000
DATABASE_PATH=/data/app.db